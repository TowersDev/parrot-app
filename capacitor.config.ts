import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.towersdev.parrotApp',
  appName: 'Parrot APP',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
